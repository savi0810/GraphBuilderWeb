networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
      - frontend
    networks:
      - dev

  api:
    build:
      context: .
      dockerfile: GraphBuilderWeb/Dockerfile
    networks:
      - dev

  frontend:
    build:
      context: .
      dockerfile: GraphBuilderFrontend/Dockerfile
    networks:
      - dev