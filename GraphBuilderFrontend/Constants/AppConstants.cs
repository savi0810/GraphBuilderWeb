namespace GraphBuilderFrontend.Constants;

public static class AppConstants
{
    public static readonly string[] FunctionExamples = new[]
    {
        "sin(x)", "cos(x)", "tan(x)", "x^3",
        "abs(x)", "1/x",
        "sin(x) + cos(x)", "x^2 + 2*x + 1", "sin(x^2)",
        "tan(x)*x"
    };
}